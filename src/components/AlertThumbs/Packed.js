import { FiPackage } from 'react-icons/fi';

const Packed = () => {
	return (
		<div
			className={
				' w-16 h-20 rounded-lg bg-[#0E6742] flex justify-center items-center'
			}
		>
			<FiPackage color="#fff" size={30} />
		</div>
	);
};

export default Packed;
