import HeaderBar from '../components/HeaderBar';

const Profile = () => {
	return (
		<>
			<HeaderBar backButton={true} title="Profile" />
			<div className="bg-white h-full"></div>
		</>
	);
};

export default Profile;
